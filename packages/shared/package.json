{
  "name": "@airos/shared",
  "private": true,
  "version": "0.1.0",
  "description": "Wire protocol and cross-cutting types shared between the AIR OS client and server.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json"
  }
}
